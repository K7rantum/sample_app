# something goes here
# must find code
# and replace here
RSpec.configure do |config|
  # stuff also goes in
  # between here, must
  # find code for this
  def test_sign_in(user)
    controller.sign_in(user)
  end
end